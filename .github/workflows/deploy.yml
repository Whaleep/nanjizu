name: Deploy to Hostinger

on:
  push:
    branches:
      - main  # 請確認您的分支名稱

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
      # 1. 取得程式碼
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      # 2. 設定 Node.js 環境 (為了編譯 Vite)
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 3. 編譯前端資源 (Vite)
      # 這裡我們只做前端 build，產生 public/build
      - name: ⚡ Build Assets
        run: |
          npm install
          npm run build

      # 4. FTP 同步檔案 (排除 vendor)
      - name: 📂 Sync files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          # Hostinger 的路徑
          server-dir: /domains/sienna-beaver-875544.hostingersite.com/laravel_core/

          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/vendor/**
            .env
            .env.example
            storage/*.key
            tests/**

      # 5. 透過 SSH 執行 Laravel 指令 (Composer & Artisan)
      - name: 🚀 Run Commands via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT }}
          # 增加 timeout 時間，以免 composer 跑太久斷線
          timeout: 60s
          script: |
            # 進入核心目錄
            cd domains/sienna-beaver-875544.hostingersite.com/laravel_core

            # 在伺服器端執行 Composer ===
            composer install --no-dev --optimize-autoloader

            # 執行遷移
            php artisan migrate --force

            # 清除快取 (Inertia 開發者建議：部署後最好重啟 queue 或清空快取)
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache

            # 確保 storage link 存在
            php artisan storage:link || true

